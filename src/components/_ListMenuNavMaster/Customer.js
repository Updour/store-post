/* Core */
import React, { Component } from 'react';

/* Presentational */
import { View, TouchableOpacity, StatusBar, ToastAndroid, Image, Modal } from 'react-native';
import { 
  Container, Header, Left, Body, Title, Content, Text
} from "native-base";
// import styles from './styles';

export default class Customers extends Component {
  render() {
    return (
      <Container>
      	<Content>
      		<Text>this Customers</Text>
      	</Content>
      </Container>
    );
  }
}
